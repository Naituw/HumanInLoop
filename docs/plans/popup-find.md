# 计划：弹窗页内查找

> 规格：`docs/specs/popup-find.md`
> 状态：代码已合入工作区，待 install + 真机验收

## 步骤

1. ~~`src/lib/findInDom.ts` + 单测~~
2. ~~`usePopupFind.ts` + `FindBar.vue`~~
3. ~~模板 `data-find-seg`~~
4. ~~`usePopupCore` 接入；`shortcutConflict` 保留 ⌘/⌃F~~
5. ~~i18n + `popup.css`~~
6. `./scripts/install.sh` 真机冒烟
